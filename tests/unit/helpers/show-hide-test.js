import { showHide } from 'adam-gedney/helpers/show-hide';
import { module, test } from 'qunit';

module('Unit | Helper | show hide');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showHide([42]);
  assert.ok(result);
});
